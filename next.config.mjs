/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "apps.apple.com",
      "cdn.metro-group.com",
      "online.metro-tr.com",
      "storage.googleapis.com",
      "cdn.online.metro-tr.com",
    ],
  },
};

export default nextConfig;
